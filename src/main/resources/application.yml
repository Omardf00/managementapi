spring.profiles.active: local

spring:
  datasource:
    url: jdbc:mysql://192.168.1.146:3306/management?useSSL=false
    username: umpalumpy
    password: Seguridad_6
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: none

logging:
  level:
    root: INFO 